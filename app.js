const modal=document.querySelector('#lang');
document.querySelectorAll('[data-l]').forEach(b=>b.onclick=()=>modal.close());
document.querySelector('#open-lang').onclick=()=>modal.showModal();
modal.showModal();
const file=document.querySelector('#file');
file.onchange=()=>{const f=file.files[0];if(!f)return;const r=new FileReader();r.onload=async()=>{const image=r.result,card=document.querySelector('#analysis-card');document.querySelector('#preview').src=image;document.querySelector('#preview').style.display='block';document.querySelector('#analysis-image').src=image;document.querySelector('#empty-analysis').classList.add('hide');card.classList.remove('hide');card.querySelector('.label').textContent='AI ANALYZING...';card.querySelector('h3').textContent='사진을 분석하고 있습니다';card.querySelector('ol').innerHTML='<li>잠시만 기다려 주세요.</li>';document.querySelector('#analysis').scrollIntoView({behavior:'smooth'});try{const res=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image})}),d=await res.json();if(!res.ok)throw Error(d.error);card.querySelector('.label').textContent=d.category;card.querySelector('h3').innerHTML=`${d.item}<br><span>${d.summary}</span>`;card.querySelector('ol').innerHTML=d.steps.map(x=>`<li><b>${x}</b></li>`).join('');card.querySelector('.notice').innerHTML='<b>주의사항</b><br>'+d.warnings.map(x=>'• '+x).join('<br>')}catch(e){card.querySelector('.label').textContent='ANALYSIS ERROR';card.querySelector('h3').textContent='분석에 실패했습니다';card.querySelector('ol').innerHTML='<li>'+e.message+'</li>'}};r.readAsDataURL(f)};
